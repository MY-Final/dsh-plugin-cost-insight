/**
 * 客户端半边共用的标识常量。
 * 改名时保持 package.json 的 `name`、src/index.ts 的 `name`、cordis.patch.yml 的
 * `id`/`name` 与此处一致。
 * @module dsh-plugin-cost-insight/client/constants
 */

/** 插件名：settings 命名空间、设置页条目 id 的共用标识。 */
export const NAMESPACE = 'dsh-plugin-cost-insight'
